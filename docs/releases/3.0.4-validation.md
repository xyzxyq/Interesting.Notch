# 3.0.4 发布验证

- Release 构建：`boringNotch` scheme、arm64，Xcode Release，版本 3.0.4 / build 304。
- Swift Codex 状态、轨迹及绘制检查通过；Python 桥接回归检查和上游更新隔离检查通过。
- 最终 DMG：`hdiutil verify` 通过；只读挂载确认应用与 `/Applications` 快捷方式；签名严格校验通过。
- 从最终 DMG 提取到本地目录后再次验证签名、版本与正式 bundle identifier；启动进程持续运行超过 45 秒。
- 独立桥接 ZIP 的 Python 脚本与提交源码逐字节一致；提供 SHA256SUMS.txt。
- README 的四组 GIF 均为 100 帧、5 秒，640×360；检查帧数、尺寸并查看中间帧，包含火箭彩带、水滴、音乐边缘、纸飞机。
- GIF 是原生绘制组件的合成演示，非桌面录屏；未将此次发布检查描述为真实任务完成端到端录屏验证。
- 使用本地开发证书，未获 Developer ID 公证。
