# 《后来》拼音歌词修复

证据：网易云播放刘若英《后来》，专辑《祝你幸福》，339.3364375 秒。原自动缓存来源 LRCLIB，歌词为带声调拼音，故障不在中文简繁转换。

修复：共享自动匹配函数排除中文标题歌曲的明显带声调拼音转写：至少 20 个字母、8 个声调元音、声调元音比例大于 8%、存在拼音特征符号且汉字比例低于 5%。该规则用于自动获取和自动缓存复核；手动选择不覆盖。此规则是保守识别，不能识别所有无声调拼音，不宣称完整语言识别。

验证：
- LyricsRepositoryChecks 覆盖拼音与汉字候选共存、旧自动缓存拒绝、手动缓存保留、普通英文不误判，通过。
- 实时重新获取得到网易云汉字歌词，共 69 行，自动保存替换本曲原缓存。
- 完整 Debug build 与稳定签名、安装校验通过。
- 重启后真实灵动岛 AX 显示“后来终于在眼泪中明白”，截图显示简体汉字。
